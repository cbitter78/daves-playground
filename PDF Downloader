import pandas as pd
import requests
import time


linkread = pd.read_excel(r'C:\Users\Alexander\Documents\cali_scrape\links.xlsx', sheet_name= 'Sheet1')
#nameread = pd.read_excel(r'C:\Users\Alexander\Documents\cali_scrape\names.xlsx', sheet_name= 'Sheet1')
link_list = linkread.values.tolist()
#name_list = nameread.values.tolist()



for rows in link_list:
    for cols in rows:
        links = cols

    dwnurl = links
    req = requests.get(dwnurl, verify = False)
    x = 1
    filename = x + 1 +'.pdf'
    
    with open(filename, 'wb') as f:
        f.write(chunk)
        print(filename)
